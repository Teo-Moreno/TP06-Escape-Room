@{
ViewData["Title"] = "Guardados";
}


<div class="saved-container">

    <h1>PARTIDAS GUARDADAS</h1>

    <div class="save-slots">

        @for (int i = 1; i <= 3; i++)
        {
            if (@ViewBag.SaveSlots[i-1] != null){
                <div class="save-slot">
                    <h2>PARTIDA @i</h2>

                    <p>Guardada en la sala @i</p>

                    <a href="@Url.Action("Cargar", "Game", new { id = i })">
                        CARGAR PARTIDA
                    </a>
                </div>
            }
            else{
                <div class="save-slot">
                    <h2>PARTIDA @i</h2>

                    <p>Vacía</p>

                    <a href="@Url.Action("NuevaPartida", "Game", new { numeroPartida = i })">
                        NUEVA PARTIDA
                    </a>
                </div>
            }

        }


    </div>

</div>